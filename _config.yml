# This is the default format. 
# For more see: http://jekyllrb.com/docs/permalinks/
permalink: /:year/:month/:title 
exclude: [".rvmrc", ".rbenv-version", "README.md", "Rakefile", "changelog.md", "deploy.sh", ".sass-cache"]
markdown: rdiscount
# enables SmartyPants (nice curly quotation marks, em/en dashes, elipses, etc):
rdiscount:
  extensions: [smart]
# Themes are encouraged to use these universal variables 
# so be sure to set them if your theme uses them.
title: benmatheja.de
tagline: personal space
author:
  name: Ben Matheja
  email: post@benmatheja.de
  github: BenMatheja
  twitter: benmatheja

production_url: https://benmatheja.de
  
# Sass settings
sass:
     sass_dir: _assets
port: 4000
lsi: false
permalink: /:year/:month/:title
safe: false

gems:
  - jekyll-sitemap
